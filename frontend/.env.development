# Dev: CRA dev server runs on :3000, backend on :8000. Production builds omit
# these and default to the page's own origin (see src/lib/api.ts).
REACT_APP_API_BASE=http://localhost:8000
REACT_APP_WS_URL=ws://localhost:8000/ws/live
