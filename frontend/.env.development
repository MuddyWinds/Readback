# Dev: Vite dev server runs on :3000, backend on :8000. Production builds omit
# these and default to the page's own origin (see src/lib/api.ts).
VITE_API_BASE=http://localhost:8000
VITE_WS_URL=ws://localhost:8000/ws/live
